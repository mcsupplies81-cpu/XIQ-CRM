# dependencies
node_modules

# build output
dist

# vercel
.vercel

# env files (contain secrets — never commit)
.env
.env.*
!.env.example

# logs
*.log

# OS
.DS_Store
.env*

# Gmail credentials — never commit
outbound/service-account.json
outbound/client_secret.json
